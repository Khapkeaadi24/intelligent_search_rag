def decompose_query(query: str):
    return [
        f"Overview of {query}",
        f"Recent developments in {query}",
        f"Key players and strategies in {query}",
        f"Challenges and risks in {query}",
        f"Future outlook of {query}"
    ]
